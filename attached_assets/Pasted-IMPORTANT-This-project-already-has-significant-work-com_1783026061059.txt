IMPORTANT: This project already has significant work completed. Continue from the current repository state only. Do NOT rebuild or redesign anything.

Project:
BrokerMAIL AI

Current Branch:
Use the latest code already committed to GitHub.

========================
CURRENT STATUS
========================

The previous AI already completed most of the markdown refactor.

Completed work:

✔ Created a shared markdown package that both frontend and backend can use.

✔ Removed duplicate markdown implementations.

✔ Single Compose Email now imports the shared markdown renderer instead of maintaining its own rules.

✔ Backend email-html.ts now uses the shared markdown renderer.

✔ TemplateEditor has already been partially migrated to the shared renderer.

✔ All backend/frontend typechecks passed successfully.

✔ API server compiled successfully.

✔ Frontend compiled successfully.

The previous AI stopped ONLY because the monthly AI quota expired.

========================
GOAL
========================

Finish the markdown refactor.

The Templates editor must behave IDENTICALLY to the Single Compose Email page.

The Compose Email page is the source of truth.

Do NOT invent new markdown parsing.

Reuse the existing shared renderer everywhere.

========================
REQUIREMENTS
========================

When the user types:

**Bold Text**

it should render immediately in:

• Plain Preview
• HTML Preview
• Final generated email

The database MUST continue storing:

**Bold Text**

NOT HTML.

HTML conversion should happen only during rendering.

Variables must continue working:

{name}
{price}
{pickup}
{delivery}
{vehicle}
{quote_id}
{email}

CTA buttons must continue working.

Email signatures must continue working.

Do NOT change storage format.

========================
VERIFY
========================

Confirm these all work:

Example:

Limited Time Offer

Your shipment from {pickup} to {delivery} is available.

Today's price is ${price}.

Reply today to secure your shipment.

Expected:

• Plain Preview shows bold formatting.
• HTML Preview shows <strong>.
• Final email received by recipient shows bold formatting.
• Database still stores markdown (**text**).

========================
FINAL TASK
========================

Search the repository for ANY remaining markdown implementation.

Replace every duplicate implementation with the shared renderer.

There should be exactly ONE markdown rendering implementation in the entire project.

========================
AFTER CODING
========================

Do NOT stop after compiling.

Verify:

1. Template Create
2. Template Edit
3. Plain Preview
4. HTML Preview
5. Final sent email
6. Variables
7. CTA buttons
8. Signature
9. Gmail Draft flow
10. SMTP flow

Only report success after all of those have been verified.

If any place still uses its own markdown parser, replace it with the shared renderer.